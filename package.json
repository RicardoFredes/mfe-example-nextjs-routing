{
  "name": "mfe-example-nextjs-routing",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "remote": "yarn workspace remote build && yarn workspace remote serve",
    "host": "yarn workspace host dev"
  }
}
